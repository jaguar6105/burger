<li>
    {{burger_name}}

    {{#if devoured}}
    <p>Burger Eaten</p>
    {{else}}
    <button class="change-eat" data-id="{{id}}" data-devoured="{{devoured}}">
        Eat
    </button>
    {{/if}}
</li>